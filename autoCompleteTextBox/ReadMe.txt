Here will be uploaded different code example top implement an auto complete textbox

Therefore, some modifications will have to be made in order to make it work in our project, such as in the javascript,
changing the static entry array to an array filled with the DB entries.

Those change modifications will be marked in the files as commentaries.
